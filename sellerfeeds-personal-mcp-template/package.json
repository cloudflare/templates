{
	"name": "sellerfeeds-personal-mcp-template",
	"description": "A personal MCP gateway that queues browser extension tools with D1, R2, and Durable Objects.",
	"cloudflare": {
		"label": "Personal MCP for Browser Tools",
		"products": [
			"Workers",
			"D1",
			"Durable Objects"
		],
		"categories": [
			"ai"
		],
		"bindings": {
			"OWNER_TOKEN": {
				"description": "A unique secret of at least 25 characters. Copy the deployment key from your SellerFeeds extension's Personal MCP settings; never commit it to Git."
			}
		}
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.21.3",
		"vitest": "4.1.10",
		"wrangler": "4.136.1"
	},
	"private": true,
	"type": "module",
	"scripts": {
		"cf-typegen": "wrangler types",
		"check": "node --check src/index.mjs && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"test": "vitest run"
	}
}
